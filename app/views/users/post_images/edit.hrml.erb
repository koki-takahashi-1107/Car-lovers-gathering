<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  		<h1>Editing Post</h1>
  		<% if @post_image.errors.any? %>
      <div id="error_explanation">
        <h3><%= @post_image.errors.count %>error prohibited this obj from being saved:</h3>
        <ul>
          <% @post_image.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
      <% end %>
    	<%= render 'layouts/errors', obj: @post_image %>
  		<%= link_to 'Show', post_image_path(@post_image), class: "post_image_#{@post_image.id}" %> 
  		<%= link_to 'Back', post_images_path, class: "back" %>
  </div>
</div>